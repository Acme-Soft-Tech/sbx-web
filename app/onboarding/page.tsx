import { Onboarding } from '@/ui/onboarding/Onboarding'

// Thin shell. Imports one component and nothing else — the real repo's convention.
export default function OnboardingPage() {
  return <Onboarding />
}
