See AGENTS.md. The enforced policy lives in `sbx-sdlc/.claude/skills/` and its hooks —
this repo is one of the three the orchestrator observes.

Before any commit: you are on a fork. PRs target `Acme-Soft-Tech/sbx-web:main`.
Commit subjects carry the Linear key, e.g. `feat(onboarding): add progress bar (SBX-5)`.
